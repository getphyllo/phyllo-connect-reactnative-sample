const clientId = '<clientId>' // add your Id here
const clientSecret = '<client Secret>' // add your client Secret
const env = "sandbox" // add your environment type, sandbox, production are the valid values

export default {
  clientId,
  clientSecret,
  env,
}
